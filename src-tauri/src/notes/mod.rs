//! NoteGenerator — Anthropic Messages API (`claude-haiku-4-5`, Sonnet 4.6
//! optional) folds transcript + scratch into structured notes (SPEC §7).
//! Local Ollama behind the same interface later (M8). Stub only in M0.

#![allow(dead_code)]
