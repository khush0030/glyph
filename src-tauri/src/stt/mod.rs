//! Transcriber — ElevenLabs Scribe v2 Realtime WS client (cloud default, M2);
//! local Whisper drops in behind the same interface (Private Mode, M8).
//! language = multi, never translate. Stub only in M0.

#![allow(dead_code)]
